.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
.vscode/settings.json
*.lnk
.DS_Store
*.alias
*~

__pycache__
# qodo can add a lot of cache data
.qodo/
# we need to keep the configuration, so this makes sure that file is not ignored
!/.qodo/qodana.yml
*.log

# Assume any folder or files containing "private" are credentials
*private*
*Private*

# Python virtual environment
.venv/

*.code-workspace
